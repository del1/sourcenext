<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1"
	android:orientation="vertical" android:background="#ccc">


    <LinearLayout
        android:id="@+id/contacts_inputSearch_container"
        android:layout_gravity="bottom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#fff"
        android:minHeight="48dp">

        <EditText android:id="@+id/contacts_inputSearch"
            android:layout_gravity="bottom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:hint="@string/cc_search"
            android:inputType="textVisiblePassword"/>
    </LinearLayout>

	<ListView android:id="@id/android:list" android:layout_width="match_parent"
		android:layout_height="0dp" android:layout_weight="1"
		android:choiceMode="multipleChoice" android:drawSelectorOnTop="false"
        android:padding="12dp" android:clipToPadding="false" android:background="#ccc"/>

    <View android:layout_width="match_parent" android:layout_height="1dip"
        android:background="#ccc" />

    <LinearLayout android:id="@+id/contacts_select_container"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView android:id="@+id/contacts_selected_text"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="match_parent"
            android:padding="12dp" android:text="@string/cc_contacts"
            android:singleLine="true"
            android:textColor="#eee" android:textSize="14sp"
            android:gravity="left|center_vertical" android:background="@drawable/agent_button_dark" />
    </LinearLayout>

</LinearLayout>