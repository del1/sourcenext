<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/switch_item"
        android:title="@string/menu_set_state"
        android:actionLayout="@layout/agent_configure_switch"
        android:showAsAction="always"/>
    
    <!-- to do get rid of this and move it into settings when settings are done! -->
</menu>